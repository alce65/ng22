import { NativeAttrs } from './native-attrs';

describe('NativeAttrs', () => {
  it('should create an instance', () => {
    const directive = new NativeAttrs();
    expect(directive).toBeTruthy();
  });
});
